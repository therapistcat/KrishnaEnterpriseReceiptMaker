{
  "name": "receipt-maker-backend",
  "version": "1.0.0",
  "description": "Backend for Shree Ganesh Enterprises Receipt Maker",
  "main": "reciept/bin/www",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node reciept/bin/www",
    "dev": "nodemon reciept/bin/www",
    "build": "cd ../Front && npm install && npm run build && echo 'Frontend built successfully'",
    "build:full": "npm install && cd ../Front && npm install && npm run build && echo 'Full build completed'"
  },
  "dependencies": {
    "@pdftron/pdfnet-node": "^11.5.0",
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^17.0.1",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "jade": "~1.11.0",
    "mongoose": "^8.16.1",
    "morgan": "~1.9.1",
    "multer": "^2.0.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
